export default function helmet(options?: any) {
  return (req: any, res: any, next: any) => next();
}

export function rateLimit(options?: any) {
  return (req: any, res: any, next: any) => next();
}
